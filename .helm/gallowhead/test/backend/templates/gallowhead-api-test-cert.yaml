apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gallowhead-api-test-cert
  namespace: gallowhead
spec:
  secretName: gallowhead-api-test-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - test.api.gallowhead.com